
version: '3'

services:

  pokemonapp-container:
    container_name: pokemonapp-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8090:80
      #- 443:443 
    restart: always